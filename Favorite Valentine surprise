<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>To My Favorite ❤️</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Great+Vibes&family=Poppins:wght@400;600&display=swap');

    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
      font-family: 'Poppins', sans-serif;
      color: #333;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    .container {
      max-width: 700px;
      padding: 30px;
      background: rgba(255, 255, 255, 0.85);
      border-radius: 25px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.2);
      animation: fadeIn 1.5s ease-in-out;
    }

    h1 {
      font-family: 'Great Vibes', cursive;
      font-size: 4.5rem;
      color: #e74c3c;
      margin: 20px 0 10px;
      text-shadow: 2px 2px 10px rgba(231, 76, 60, 0.4);
    }

    p {
      font-size: 1.4rem;
      line-height: 1.6;
      margin: 20px 0;
    }

    .highlight {
      color: #c0392b;
      font-weight: 600;
    }

    .buttons {
      margin: 40px 0 20px;
    }

    button {
      font-size: 1.3rem;
      padding: 15px 40px;
      margin: 10px 20px;
      border: none;
      border-radius: 50px;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
    }

    #yesBtn {
      background: #e74c3c;
      color: white;
    }

    #yesBtn:hover {
      background: #c0392b;
      transform: scale(1.1);
    }

    #noBtn {
      background: #ecf0f1;
      color: #7f8c8d;
    }

    #noBtn:hover {
      background: #bdc3c7;
    }

    .heart {
      position: absolute;
      font-size: 2rem;
      animation: float 6s infinite ease-in-out;
      opacity: 0.7;
    }

    @keyframes float {
      0%,100% { transform: translateY(0) rotate(0deg); }
      50% { transform: translateY(-30px) rotate(10deg); }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.8); }
      to { opacity: 1; transform: scale(1); }
    }

    #message {
      display: none;
      font-size: 2rem;
      color: #e74c3c;
      margin-top: 40px;
      animation: fadeIn 1s;
    }

    @media (max-width: 600px) {
      h1 { font-size: 3.5rem; }
      button { padding: 12px 30px; font-size: 1.1rem; }
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Hey Favorite ❤️</h1>
    
    <p>We've been best friends for so long, sharing laughs, secrets, and all the little moments that make life beautiful.</p>
    
    <p class="highlight">This Valentine's, I want to ask something special...</p>
    
    <p>Will you be my Valentine? 💕<br>More than friends... my person.</p>

    <div class="buttons">
      <button id="yesBtn">Yes! ❤️</button>
      <button id="noBtn">No 😔</button>
    </div>

    <div id="message">
      Yayyyy! I knew my Favorite would say yes 😍<br>
      Can't wait to make this Valentine's (and every day after) amazing with you! 💖
    </div>
  </div>

  <!-- Floating hearts -->
  <div class="heart" style="left:10%; top:20%; animation-delay:0s;">❤️</div>
  <div class="heart" style="left:25%; top:60%; animation-delay:1.5s;">💕</div>
  <div class="heart" style="left:40%; top:15%; animation-delay:3s;">💖</div>
  <div class="heart" style="left:70%; top:50%; animation-delay:2s;">❤️</div>
  <div class="heart" style="left:85%; top:30%; animation-delay:4s;">💞</div>

  <script>
    const yesBtn = document.getElementById('yesBtn');
    const noBtn = document.getElementById('noBtn');
    const message = document.getElementById('message');

    yesBtn.addEventListener('click', () => {
      message.style.display = 'block';
      noBtn.style.display = 'none';
      yesBtn.style.display = 'none';
      // You can add confetti or more effects here if you want later!
    });

    noBtn.addEventListener('click', () => {
      // Playfully move the No button away
      noBtn.style.transform = `translate(${Math.random()*200 - 100}px, ${Math.random()*200 - 100}px)`;
      noBtn.innerText = "Are you sure? 🥺";
    });
  </script>

</body>
</html> ADD VALENTINE PACKAGE PAGE
